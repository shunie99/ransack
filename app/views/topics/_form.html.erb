<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
    <%= f.label :担当 %><br />
    <%= f.select :user_id, User.all.map { |u| [u.name, u.id] } %><!--この行を修正-->
  </div>
  
  <div class="field">
    <%= f.label :メンバー %><br />
       <%= f.text_field :member%>
  </div>
  
  <div class="field">
    <%= f.label :重要度　%><br />
    <%= f.select :test, ['低','中','高'] %><!--この行を修正-->
    

  </div>
  
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
    
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

